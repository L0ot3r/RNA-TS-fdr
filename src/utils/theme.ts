import { DefaultTheme } from 'react-native-paper';

const theme = {
	...DefaultTheme,
	colors: {
		...DefaultTheme.colors,
		primary: '#3f19e7',
		accent: '#f1c40f',
		secondary: '#ff6060',
	},
};

export { theme };
